```
NAME:
   hbase-cmd
USAGE:
   main [global options] command [command options] [arguments...]

COMMANDS:
   create
   drop
   put
   delete
   get
   scan
   help, h  Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --zookeeper-quorum value  (default: "localhost")
   --hbase-node value        node path stored in zookeeper (default: "/hbase-unsecure")
   --table value             namespace:tableName (default: "system:dummyTable")
   --log value               log level for stdout debug/info/warn/error/fatal (default: "warn")
   --help, -h                show help
   --version, -v             print the version

VERSION:
   0.1.0
```
